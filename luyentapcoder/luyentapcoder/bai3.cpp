/*
#include<iostream>
#include<cmath>
using namespace std;

int main() {

	/*
	float x = 2.0, y = 3;
	float z = 1 / (x + 1 / (1/(x + y) + x));
	float a = pow(3 * x + 2 * y, 2);
	float b = pow(5 * x + 1, 3);
	cout << x * y + x / y << "\n";
	cout << x + z << "\n";
	cout << sqrt(a * b);
	*/

	/*
	float r=0;
	const float pi = 3.14;
	cout << "nhap ban kinh:";
	cin >> r;

	cout << "chu vi hinh tron la :" << 2 * pi * r;
	cout << "\ndien tich hinh tron la :" << pi * r * r;
	*/
	/*
	float a=0, b=0, c=0;
	float p=0, s=0;
	cout << "nhap canh a: " ;
	cin >> a;
	cout << "nhap canh b: " ;
	cin >> b;
	cout << "nhap canh c: ";
	cin >> c;
	p = (a + b + c) / 2;
	s = sqrt(p * ((p - a) * (p - b) * (p - c)));

	cout << "dien tich tam giac la " << s;
	*/
	/*
	int n;
	int x;
	cout << "nhap vao so n:";
	cin >> n;
	cout << "so dao nguoc: ";
	while (n > 0) {
		x = n % 10;
		cout << x;
		n /= 10;
	}
	*/
	/*
	bool a = 1;
	bool b = 1;
	bool c = a && b;
	bool d = a || b;
	bool e = a;
	bool f = (a && !b) || (b && !a);


	int x = 0;
	bool a = (x >= 0);
	bool b = ((x >= -1 && x <= 2) || (x <= 6 && x>=4));
	bool c = ((x<2 && x>-2) || (x = 5));
	bool d = ((x > -4 && x <= 0) || (x > 4));

	cout << a << "\n" << b << "\n" << c << "\n" << d;
}


// ep kieu string
int x = 1;
string s = "123d89";		string s = "123.4";   (in ra so thap phan);
x = stoi(s);				x = stof(s);
cout << x;
Note: ket thuc tu sau chu cai.

// int x=1;
string s = to_string(x);
cout << s;
}*/



