/*#include<iostream>
using namespace std;
float the_tich(float a, float b, float h) {
	return a * b * h;
}
float chu_vi(float a, float b, float c) {
	return 4 * (a + b + c);
}
float dien_tich_xq(float a, float b, float h) {
	return (2 * a + 2 * b) * h;
}
float dien_tich_tp(float a, float b, float h) {
	float stp = (2 * a + 2 * b) * h + 2 * a * b;
	return stp;
}

int main()
{
    float a, b, h;
    std::cout << "Nhap lan luot a, b, h: ";
    std::cin >> a >> b >> h;
    std::cout << "Ket qua 1 - The tich: " << the_tich(a, b, h) << "\n";
    std::cout << "Ket qua 2 - Dien tich xung quanh: " << dien_tich_xq(a, b, h) << "\n";
    std::cout << "Ket qua 3 - Dien tich toan phan: " << dien_tich_tp(a, b, h) << "\n";
    std::cout << "Ket qua 4 - Chu vi: " << chu_vi(a, b, h) << "\n";
    return 0;
}*/