/*#include<iostream>
using namespace std;
int main()
{
	int km = 0;
	int x, y, z;
	cout << "nhap so km ban muon di : ";
	cin >> km;

	if (km == 1)
	{
		x = 15000;
		cout << "so tien phai tra: " << x;
	}
	else if (km >= 2 && km <= 30)
	{
		y = 13000;
		int tien = 13000 * (km - 1) + 15000;
		cout << "so tien phai tra la: " << tien;
	}
	else
	{
		z = 10000;
		int tien = 13000 * 29 + 15000 + z * (km - 30);
		cout << "so tien phai tra la: " << tien;
	}
}
*/