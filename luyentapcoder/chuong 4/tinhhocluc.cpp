/*#include<iostream>
using namespace std;
int main()
{
	float a, c, b, d, e, f, g;
	cout << "Toan:";
	cin >> a;
	cout << "Van:";
	cin >> b;
	cout << "Anh:";
	cin >> c;
	cout << "Ly:";
	cin >> d;
	cout << "Hoa:";
	cin >> e;
	cout << "Su:";
	cin >> f;
	cout << "Dia:";
	cin >> g;
	
	float tb = (a + b + c + d + e + f + g) / 7;
	cout << "diem trung binh : " << tb << "\n";


	if (tb < 0 || tb>10 || a < 0 || b < 0 || c < 0 || d < 0 || e < 0 || f < 0 || g < 0 || a > 10 || b >10 || c > 10 || d > 10 || e > 10 || f > 10 || g > 10)
	{
		cout << "diem trung binh khong hop le.";
	}
	else
	{
		if (a * b * c * d * e * f * g == 0 || tb <4)
		{
			cout << "hoc luc yeu";
		}
		else if (tb >= 8 && (a > 4 && b > 4 && c > 4 && d > 4 && e > 4 && f > 4 && g > 4))
		{
			cout << "hoc luc gioi";
		}
		else if (tb >= 8 && (a < 4 || b < 4 || c < 4 || d < 4 || e < 4 || f < 4 || g < 4))
		{
			cout << "hoc luc kha";
		}
		else if ((tb > 6.5 && tb < 8) && (a > 3 && b > 3 && c > 3 && d > 3 && e > 3 && f > 3 && g > 3))
		{
			cout << "hoc luc kha";
		}
		else if ((tb > 6.5 && tb < 8) && (a < 3 || b < 3 || c < 3 || d < 3 || e < 3 || f < 3 || g < 3))
		{
			cout << "hoc luc trung binh";
		}
		else if (tb <= 6.5 && tb >= 4)
		{
			cout << "hoc luc trung binh";
		}

	}

	*/



}